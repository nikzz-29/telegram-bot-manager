# Secrets: `.env` holds a live BOT_TOKEN. `.env.example` is the tracked template.
.env
.env.*
!.env.example
!.env.production.example

# Database dumps. `docs/deploy.md` has you run `pg_dump` from the repository
# root, and the result is every chat's data — the one file here that must not
# reach a remote by accident.
backup-*.sql
backup-*.sql.gz

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / Mini App
node_modules/
dist/
*.tsbuildinfo

# OS
.DS_Store
