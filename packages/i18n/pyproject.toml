[project]
name = "tg-manager-i18n"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fluent.runtime>=0.4.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
